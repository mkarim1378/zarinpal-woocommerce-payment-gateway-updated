# تغییرات پلاگین ووکامرس زرین‌پال

## نسخه 5.0.20 - تکمیل داده‌های ادغام با چک‌اوت بلاکی ووکامرس

در `includes/class-wc-zarinpal-gateway-blocks-support.php`، متد
`get_payment_method_data()` فقط `title`, `description` و `icon` را برمی‌گرداند،
در حالی که `assets/js/index.js` (خط 32) از `ZP_settings.supports` برای تعیین
`features` روش پرداخت در رجیستری بلاک‌ها استفاده می‌کند. در نتیجه در چک‌اوت
بلاکی، قابلیت‌هایی مانند refunds/subscriptions ممکن بود به‌درستی برای این
درگاه ثبت نشوند.

**راه‌حل:** کلیدهای `fee_payer` و `supports` (با همان منطق
`array_filter($this->gateway->supports, ...)`) به خروجی `get_payment_method_data()`
اضافه شدند.

## نسخه 5.0.19 - رفع دو باگ در تایید پرداخت و استرداد وجه

### مشکل ۱: کد بازگشتی 101 (تراکنش قبلاً تایید شده) به‌عنوان شکست تلقی می‌شد
متد `verifyPayment` در `ZarinpalHelperClass.php` هم کد 100 و هم 101 را موفق
می‌داند، اما در `Return_from_ZarinPal_Gateway` و `zpal_manual_verify_transaction`
فقط کد 100 بررسی می‌شد. در نتیجه اگر زرین‌پال 101 برمی‌گرداند (مثلاً کاربر صفحه
بازگشت را رفرش می‌کرد)، سفارش کامل نمی‌شد و وارد مسیر بازیابی on-hold یا فقط
نمایش پیام اطلاع‌رسانی می‌شد.

**راه‌حل:** هر دو کد 100 و 101 اکنون موفق در نظر گرفته می‌شوند و سفارش با
`payment_complete()` تکمیل می‌شود؛ برای 101 یک یادداشت سفارش جداگانه
("این تراکنش قبلاً تایید شده بود") ثبت می‌شود.

### مشکل ۲: تبدیل واحد پول اشتباه در استرداد وجه
در `process_refund()`، مبلغ استرداد همیشه در 10 ضرب می‌شد (فرض بر تومان)، در
حالی که در بقیه کد بر اساس واحد پول واقعی فروشگاه (IRR/IRHR/IRT/IRHT) محاسبه
می‌شود. برای فروشگاه‌هایی با واحد پول غیر از تومان، مبلغ استرداد ارسالی به
زرین‌پال اشتباه بود.

**راه‌حل:** `process_refund()` اکنون از همان منطق تبدیل واحد پول استفاده می‌کند.

## بدون انتشار نسخه - پاکسازی فایل‌های مرده و تکراری

فایل‌ها و پوشه‌های زیر که هیچ‌جای کد به آن‌ها ارجاع داده نمی‌شد و نسخه
قدیمی/تکراری فایل‌های فعال بودند حذف شدند:

- `ZarinPal.php` (کلاس قدیمی که جایگزینش `ZarinpalHelperClass.php` شده بود)
- `class-wc-zarinpal-gateway-blocks-support.php` در ریشه پروژه (نسخه قدیمی؛ نسخه فعال در `includes/` قرار دارد)
- پوشه `images/` و `js/` در ریشه پروژه (نسخه قدیمی و تکراری `assets/images/` و `assets/js/`)
- تصاویر استفاده‌نشده `assets/images/logo.png` و `logo_1.png` تا `logo_10.png` (فقط `logo.svg` در کد استفاده می‌شود)
- `CONTEXT_HANDOFF.md` (فایل موقتی انتقال context)

## نسخه 5.0.18 - رفع مشکل ناتمام ماندن سفارش بعد از پرداخت موفق

### مشکل:
تراکنش‌هایی که در درگاه زرین‌پال با موفقیت ثبت می‌شدند، در صورت بروز خطا یا timeout در درخواست `verifyPayment` (با وجود کسر شدن وجه توسط بانک)، در سایت به صورت «ناموفق / در انتظار پرداخت» باقی می‌ماندند.

### راه‌حل:
- در `class-wc-gateway-zarinpal.php`، در صورت شکست `verifyPayment`، با فراخوانی `inquiryPayment($authority)` وضعیت واقعی تراکنش از زرین‌پال استعلام می‌شود.
- اگر وضعیت استعلام `PAID` یا `VERIFIED` باشد، سفارش به وضعیت **«در انتظار بررسی» (on-hold)** تغییر می‌کند (به‌جای ماندن در `pending` که ممکن است بعداً به‌صورت خودکار لغو شود) و یک یادداشت سفارش با جزئیات ثبت می‌شود تا ادمین از طریق دکمه «بررسی دستی تراکنش» آن را نهایی کند. کاربر هم پیام موفقیت می‌بیند و به صفحه تشکر هدایت می‌شود.
- اگر استعلام هم پاسخی پیدا نکند، رفتار قبلی (نمایش پیام شکست) حفظ شده است.

## نسخه جدید - ویژگی کسر کارمزد از خریدار

### ویژگی‌های جدید:

1. **انتخاب کسر کارمزد از پذیرنده یا خریدار**
   - در تنظیمات پلاگین، گزینه جدید "کسر کارمزد از" اضافه شده
   - دو گزینه موجود: "پذیرنده (پیش‌فرض)" و "خریدار"

2. **محاسبه خودکار کارمزد**
   - اگر "کسر کارمزد از خریدار" انتخاب شود، کارمزد به صورت خودکار محاسبه می‌شود
   - استفاده از API محاسبه کارمزد زرین‌پال (`feeCalculation.json`)

3. **نمایش کارمزد در صفحه چک‌اوت**
   - کارمزد به عنوان یک ردیف جداگانه در صورت‌حساب نمایش داده می‌شود
   - نمایش پیام اطلاع‌رسانی برای کاربر

4. **پرداخت با مبلغ پیشنهادی**
   - استفاده از `suggested_amount` برای ارسال به درگاه
   - تضمین دریافت مبلغ دقیق پس از کسر کارمزد

5. **تأیید پرداخت با مبلغ صحیح**
   - تأیید تراکنش با `suggested_amount` در صورت کسر کارمزد از خریدار
   - حفظ سازگاری با روش‌های قبلی

6. **نمایش بهتر کارمزد در checkout**
   - نمایش مجزای کارمزد در خلاصه سفارش
   
7. **تشخیص خودکار نوع کسر کارمزد**
   - بعد از نصب/آپدیت پلاگین، تشخیص خودکار نوع کسر کارمزد
   - سینک شدن با تنظیمات حساب زرین‌پال

### تغییرات فنی:

- **ZarinpalHelperClass.php**: اضافه شدن متد `calculateFee()`
- **class-wc-gateway-zarinpal.php**: 
  - اضافه شدن فیلد تنظیمات `fee_payer`
  - متدهای `add_zarinpal_fee()` و `add_fee_notice()`
  - تغییر منطق ارسال و تأیید پرداخت
- **assets/js/index.js**: اضافه شدن JavaScript برای مدیریت تغییرات checkout

### نحوه استفاده:

1. به تنظیمات پلاگین زرین‌پال بروید
2. در بخش "تنظیمات عملیات پرداخت"، گزینه "کسر کارمزد از" را پیدا کنید
3. "خریدار" را انتخاب کنید
4. تنظیمات را ذخیره کنید

از این پس، کارمزد تراکنش به صورت‌حساب مشتری اضافه خواهد شد.

### نکات مهم:

- کارمزد فقط زمانی محاسبه می‌شود که روش پرداخت زرین‌پال انتخاب شده باشد
- محاسبه کارمزد با استفاده از API رسمی زرین‌پال انجام می‌شود
- در صورت خطا در محاسبه کارمزد، فرآیند خرید متوقف نمی‌شود
- داده‌های کارمزد در متادیتای سفارش ذخیره می‌شوند

### ویژگی‌های امنیتی:

- **عدم کش کردن**: هیچ داده‌ای در session کش نمی‌شود تا از دستکاری جلوگیری شود
- **محاسبه لحظه‌ای**: کارمزد دقیقاً قبل از ارسال به درگاه محاسبه می‌شود
- **اعتبارسنجی مبلغ**: حداکثر 10% افزایش مبلغ مجاز است
- **Timestamp امنیتی**: داده‌های کارمزد حداکثر 1 ساعت معتبر هستند
- **تطبیق مبلغ**: مبلغ اصلی با مبلغ ذخیره شده تطبیق داده می‌شود
- **ذخیره سمت سرور**: تمام داده‌ها در متادیتای سفارش (سمت سرور) ذخیره می‌شوند

## نسخه 5.0.17
- بهبود امنیت در فرآیند بازگشت از درگاه پرداخت
- بهبود پایداری در پردازش تراکنش‌ها

## نسخه 5.0.16
- افزودن ویژگی کسر کارمزد از خریدار
- انتخاب کسر کارمزد از پذیرنده یا خریدار در تنظیمات
- محاسبه خودکار کارمزد با استفاده از API زرین‌پال
- نمایش کارمزد در صفحه چک‌اوت
- پرداخت با مبلغ پیشنهادی و تضمین دریافت مبلغ دقیق
- تشخیص خودکار نوع کسر کارمزد و سینک با تنظیمات حساب
- رفع مشکلات سازگاری با HPOS (High-Performance Order Storage)
- بهبود نمایش کارمزد در خلاصه سفارش

## نسخه 5.0.1
- افزودن حالت تست (سندباکس)
- افزودن امکان استرداد تراکنش
- افزودن امکان مشاهده جزئیات تراکنش
- نمایش تراست لوگو زرین پال در پنل تنظیمات
- سازگاری با وردپرس 6.7.1 و ووکامرس 9.4.1
- تست شده بر روی ووکامرس 9.4.1

## نسخه 4.9.3
- بروزرسانی در ارسال شماره همراه و سازگاری با سایر پلاگین‌های دریافت شماره همراه.

## نسخه 4.9.2
- رفع مشکل ارسال شماره همراه با پیشوند‌های +98 و سایر نواقص آن‌ها.
- افزوده شدن لوگوی جدید زرین پال.

## نسخه 4.9.1
- هماهنگ‌سازی ارسال شماره همراه و ایمیل خریدار.
- هماهنگ‌سازی ارسال شماره سفارش `order_id`.

## نسخه 4.9
- تغییرات در بخش تنظیمات درگاه.
- بهبود عملکرد در صورت خطای اتصال به سرور زرین پال.
- سازگاری با نسخه جدید ووکامرس و وردپرس.

## نسخه 4.8.1
- بروزرسانی به نسخه جدید و رفع خطاها.

## نسخه 4.7.3
- هماهنگ‌سازی بخش واحد ارز با واحد ارز در زرین پال.
- ارتقا به وب سرویس جدید زرین پال.

## نسخه 4.7.2
- بروزرسانی واحد.

## نسخه 4.7.1
- رفع خطاها.

## نسخه 4.7
- بروزرسانی به وب سرویس جدید زرین پال.
- درج شماره همراه و ایمیل در درگاه بانکی.

## نسخه 4.6.1
- رفع خطای -۱ در برخی شرایط.

## نسخه 4.6
- بهینه‌سازی کدها و سازگار سازی با نسخه‌های جدید ووکامرس.

## نسخه 4.5
- افزودن تنظیم دستی درگاه برای انتقال به وب گیت و زرین گیت.
- تغییر نوع وب سرویس از SOAP به REST.
- رفع باگ تایید نشدن سفارش پس از پرداخت موفق در برخی حالات.

## نسخه 4.4
- اضافه شدن خودکار واحدهای پولی ریال، تومان، هزار ریال و هزار تومان به ووکامرس.

## نسخه 4.3.5
- افزونه به صورت پیش‌فرض روی درگاه مستقیم است.
- نمایش کد خطا.
- کاهش یک مرحله از پرداخت جهت سریع‌تر شدن فرآیند.

## نسخه 4.3.2
- رفع مشکل باز نشدن قسمت تنظیمات در ووکامرس جدید.

## نسخه 4.3
- رفع مشکل ریدایرکت پس از پرداخت. 